'use strict';

const STAT_BUDGET = 9;
const STAT_MIN = 1;
const STAT_MAX = 5;
const DECK_SIZE = 8;
const MAX_FIVE_CARDS = 3;
const OPENING_HAND = 4;
const GOAL_POINTS = 30;
const GUARD_BONUS = 3;
const BOND_BONUS = 1;
const RALLY_BONUS = 1;
const SMASH_BONUS = 2;

const TRAITS = ['Beast', 'Titan', 'Element', 'Machine', 'Icon', 'Spirit'];
const ABILITIES = ['Smash', 'Dodge', 'Trick', 'Rally', 'Everyman'];
const STATS = ['power', 'speed', 'wits'];

module.exports = {
  STAT_BUDGET,
  STAT_MIN,
  STAT_MAX,
  DECK_SIZE,
  MAX_FIVE_CARDS,
  OPENING_HAND,
  GOAL_POINTS,
  GUARD_BONUS,
  BOND_BONUS,
  RALLY_BONUS,
  SMASH_BONUS,
  TRAITS,
  ABILITIES,
  STATS,
};
